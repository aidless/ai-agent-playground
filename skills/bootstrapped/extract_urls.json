{
  "name": "extract_urls",
  "code": "def extract_urls(params: dict) -> str:\n    \"\"\"Extract URLs from text.\"\"\"\n    import re\n    text = params.get(\"text\", \"\")\n    urls = re.findall(r'https?://[^\\s]+', text)\n    return str(urls)",
  "description": "I need a tool to extract URLs from text but don't have one",
  "created_at": "2026-05-19T06:37:49.269219+00:00",
  "source_reflection": "I need a tool to extract URLs from text but don't have one",
  "validated": true,
  "registered": true,
  "error": ""
}